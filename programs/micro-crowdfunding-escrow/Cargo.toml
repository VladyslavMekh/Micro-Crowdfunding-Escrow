[package]
name = "micro-crowdfunding-escrow"
version = "0.1.0"
description = "Decentralized escrow smart contract for micro-crowdfunding campaigns on Solana"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "micro_crowdfunding_escrow"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
